import json

class JsonHandle:
	def __init__(self,filename="gitgit.rc.json",mode="r"):
		self.jsonfile=open(filename,mode)

	def readConfig(self):
		#read object into parms
	
	def writeConfig(self,config):
		
