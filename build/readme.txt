this is a collection of possibly libraries for git kit.

Of particular interest are the java libraries, which would let git-git run anywhere using onejar. The one issue will be pushing (rsa authentication... readup on jsch). 

->potential to link this in to birchsrv (dev mode)
